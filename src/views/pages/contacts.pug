extends ../layouts/master.pug

block title
  title Casa Bianca | Контакты

block basicSeo
  meta(name='description' content='Contacts page')
  meta(name='keywords', content='contacts, page')

block manifestBrowserconfigFiles
  link(href='../manifest.json', rel='manifest')
  meta(name='msapplication-config' content='../browserconfig.xml')

block content

  +header('contacts')

  .barba(data-barba="wrapper")
    main(data-barba="container" data-barba-namespace="contacts")
      include ../mixins/hero-item.pug
      +hero-item('/assets/images/contacts-bg.png', '#contacts')
      include ../components/offices.pug
      include ../components/map2.pug
      include ../components/clients.pug
      include ../components/contacts.pug